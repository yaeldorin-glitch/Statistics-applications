# תסריט לסרטון 3 — ניתוח שרידות: חולי סרטן ריאות (lifelines)

**קובץ המחברת:** `3_survival_analysis_lung_cancer.ipynb`
**משך משוער:** 20–25 דקות

---

## פתיחה (דקה 0:00–1:30)

זו מחברת מספר 3, האחרונה מבין השלוש, ועוסקת בניתוח שרידות (survival analysis) עם החבילה `lifelines`. בניגוד לשתי המחברות הקודמות, כאן **לא** נדרשת השוואה לגישה בייסאנית — ההנחיה מבקשת את זה רק במחברות ה-PyMC. המטרה כאן היא להראות שליטה מלאה בכלי `lifelines` על דאטה אמיתי.

הדאטהסט של המרצה היה על אריכות ימים של זבובי פירות. אני בחרתי בדאטה הרבה יותר אינטואיטיבי: `lung` — נתונים אמיתיים על חולי סרטן ריאות מתקדם, ממחקר קליני אמיתי (Loprinzi ואחרים, 1994), מובנה בתוך `lifelines` עצמה.

**תראי על המסך:** תא הטעינה, ותסבירי את המשתנים המרכזיים: `time` — כמה ימים עברו מרישום המטופל למחקר ועד למוות או לתצפית האחרונה; `status` — 1 אם המוות נצפה בפועל, 0 אם המטופל היה עדיין בחיים בפעם האחרונה שראינו אותו. זה נקרא **צנזורה מימין (right censoring)** — אנחנו יודעים שהוא שרד *לפחות* עד אותו יום, אבל לא יודעים בדיוק כמה עוד.

יש גם משתנים נוספים: גיל, מין, ומדדי תפקוד פיזי של המטופל.

**נקודה קטנה של יושר מדעי:** למשתנה `meal.cal` (צריכה קלורית) חסרים נתונים ל-21% מהמטופלים. במקום "להשלים" את החוסר בניחוש, החלטתי פשוט להוריד אותו מרשימת המשתנים לרגרסיה בהמשך ולהגיד את זה בפירוש — כי ייתכן שהחוסר עצמו לא אקראי (מטופלים חולים יותר פחות ימלאו שאלון תזונה), וכל השלמה הייתה עלולה להטות את התוצאה בלי שנדע.

---

## חלק 1: הצגת הדאטה (דקה 1:30–3:30)

**תראי על המסך:** `plot_lifetimes` על 25 מטופלים אקראיים.

תסבירי: כל קו מייצג מטופל אחד; קו שמסתיים ב"עיגול" מלא מייצג מוות שנצפה, קו דהוי יותר מייצג צנזורה — המטופל היה עדיין בחיים כשהמעקב הסתיים.

**תראי על המסך:** גרפי הכינור (violin plots) — זמן שרידות לפי מין, וזמן שרידות לפי תוצאה.

הערה קטנה שכדאי להזכיר: בגרף הכינור רואים "זנב" שנכנס לערכים שליליים, למרות שזמן שרידות הוא תמיד חיובי. זה לא טעות בדאטה — זה רק אפקט החלקה (KDE) שיש לו "זנבות" אינסופיים מבחינה מתמטית. בדקתי בפועל את הערך המינימלי בדאטה (5 ימים) כדי לוודא שאין שם באמת ערכים שליליים.

---

## חלק 2: למה אסור להתעלם מצנזורה — הדגמה כמותית (דקה 3:30–8:00)

זה החלק הכי חשוב במחברת הזו. ברירת המחדל ההיסטורית למידול זמן עד אירוע היא **התפלגות מעריכית (Exponential)** — יש לה תכונה מיוחדת שנקראת "חוסר זיכרון": הסיכוי למות ברגע הבא לא תלוי בכמה זמן כבר שרדת.

**במקום רק להגיד "אסור להתעלם מצנזורה", בדקתי כמה זה באמת משנה במספרים.**

**תראי על המסך:** הפלט של תא ההשוואה.

הפעלתי `scipy.stats.expon.fit()` בצורה נאיבית — ישירות על עמודת `time`, בלי שום מודעות לצנזורה. הפונקציה הזו "חושבת" שכל מטופל מת בדיוק ביום שראינו אותו לאחרונה, גם אם הוא בסך הכל נצפה בחיים באותו יום. זה נתן הערכת שרידות ממוצעת של כ-305 ימים.

לעומת זאת, `ExponentialFitter` מ-`lifelines`, שיודע להתחשב נכון בצנזורה, נתן כ-422 ימים.

**ההבדל: כ-28% הטיה כלפי מטה!** ההערכה הנאיבית **מזלזלת** משמעותית בזמן השרידות, כי היא מתייחסת למטופלים ששרדו הרבה יותר (אבל פשוט לא המשכנו לעקוב אחריהם) כאילו מתו בדיוק כשראינו אותם לאחרונה. זו לא רק נקודה תיאורטית — הראיתי אותה בפועל, במספרים אמיתיים מההרצה.

**תראי על המסך:** גרף השוואת ה-PDF של Exponential מול Weibull, שניהם עם התחשבות נכונה בצנזורה.

### רגע ביקורתי נוסף — פרשנות שתיקנתי

התוצאה של Weibull נתנה `rho ≈ 1.32`. בטיוטה הראשונה שלי כתבתי ש"זה קרוב ל-1, כלומר סיכון כמעט קבוע" — אבל זו טעות! `rho > 1` אומר בדיוק ההפך: הסיכון **עולה** עם הזמן. תיקנתי את זה: 1.32 זו עלייה אמיתית אבל מתונה בסיכון עם הזמן — מתאים לתיאוריה שמחלה מתקדמת מגבירה סיכון ככל שעובר הזמן, רק לא בצורה דרמטית. חשוב להראות בסרטון שגם אני עצמי טעיתי בפרשנות ראשונית ותיקנתי את זה מול המספר בפועל.

---

## חלק 3: פונקציית שרידות לא-פרמטרית (דקה 8:00–12:00)

**תראי על המסך:** `scipy.stats.ecdf` — CDF אמפירי, ו-1 פחות CDF כפונקציית שרידות.

תסבירי: גם כאן יש בעיית צנזורה — `ecdf` מתייחס לכל תצפית כאילו היא אירוע שנצפה במלואו, מה שמטה את הזנב של עקומת השרידות כלפי מטה בדיוק כמו קודם.

**הפתרון: אומדן קפלן-מאייר (Kaplan-Meier).** בכל נקודת זמן שבה נצפה מוות, האומדן מצמצם את השרידות רק לפי מי שעדיין "בסיכון" (בחיים ולא צונזר) — כך שמטופלים מצונזרים נשארים בקבוצת הסיכון בדיוק לאורך הזמן שבו נצפו, ופשוט "יוצאים" ממנה בלי לספור "מוות".

**תראי על המסך:** עקומת קפלן-מאייר עם רצועת ביטחון.

תסבירי את שיטת **גרינווד (Greenwood)** לחישוב שגיאת התקן, ואת הטרנספורמציה `log(-log(x))`: בלי הטרנספורמציה הזו, רווח סמך סימטרי סביב הסתברות שרידות קרובה ל-0 או ל-1 עלול לצאת מחוץ לתחום [0,1]. הטרנספורמציה הזו ממפה את הטווח (0,1) לכל הישר הממשי, בונים שם רווח רגיל (בעזרת "שיטת הדלתא"), וממפים בחזרה — כך שהתוצאה תמיד נשארת בתוך [0,1].

**תראי על המסך:** גרף הפונקציה `log(-log(x))`.

### חשוב: Nelson-Aalen

**תראי על המסך:** עקומת הסיכון המצטבר של Nelson-Aalen.

תסבירי למה לא פשוט לוקחים מינוס-לוג של קפלן-מאייר: `S(t) = exp(-H(t))` היא זהות מתמטית מדויקת, אבל כשמעבירים את אי-הוודאות (לא רק את האומדן עצמו) דרך פונקציה קמורה כמו מינוס-לוג, מקבלים הטיה שיטתית כלפי מעלה (אי-שוויון ינסן), בדיוק במקום שבו יש הכי מעט דאטה — בזנב. Nelson-Aalen מעריך את הסיכון המצטבר ישירות, בלי לעבור דרך הטרנספורמציה הבעייתית.

---

## חלק 4: השוואת קבוצות ורגרסיית קוקס (דקה 12:00–19:00)

**תראי על המסך:** שתי עקומות קפלן-מאייר, גברים מול נשים.

השאלה: העקומות נראות שונות — האם זה משמעותי סטטיסטית? המבחן המתאים הוא **מבחן ה-log-rank**, שמשווה מספר מקרי המוות שנצפו מול המצופה בכל קבוצה, בכל נקודת זמן.

**תראי על המסך:** תוצאת המבחן — סטטיסטי chi-squared של 10.33, p קטן מ-0.005. משמעותי מאוד.

עכשיו **רגרסיית קוקס (Cox Proportional Hazards)** — המודל המרכזי בניתוח שרידות. תסבירי:
- המודל הוא **"חצי-פרמטרי"**: יש לו פונקציית סיכון בסיס לא-מוגדרת `h_0(t)`, שמוכפלת בגורם `exp(β·x)` שתלוי במשתני המטופל.
- הטריק שמאפשר להתאים את המודל בלי לדעת בכלל את `h_0(t)`: **ה-likelihood החלקי (partial likelihood)** — בכל נקודת זמן שבה מישהו מת, שואלים רק "מתוך כל מי שעדיין בסיכון עכשיו, מה ההסתברות שדווקא זה שמת הוא זה שמת, לפי הסיכונים היחסיים שלהם" — וב-`h_0(t)` המשותף מצטמצם (מתבטל) מהיחס.
- **אין חותך (intercept)** במודל, כי כל קבוע שהיה מתווסף היה פשוט נבלע בתוך `h_0(t)` הבלתי-מוגדר ממילא — אין דרך להבחין בין השניים.

**תראי על המסך:** פלט `cph.print_summary()`.

תסבירי איך קוראים **יחס סיכון (hazard ratio)**, שהוא `exp(coef)`: ערך מעל 1 אומר סיכון גבוה יותר, מתחת ל-1 אומר הגנה. במודל שלי, `ph.ecog` (מדד תפקוד שקבע רופא, כשמספר גבוה יותר = פחות מתפקד) יצא עם יחס סיכון 1.67 ומובהקות גבוהה — ככל שהמטופל פחות מתפקד, כך הסיכון גבוה יותר, מתאים לאינטואיציה קלינית. גם המין יצא מובהק — נשים עם יחס סיכון 0.55, כלומר סיכון נמוך יותר מגברים, שמתאים גם למחקרים אמיתיים על סרטן ריאות. `concordance_index` בערך 0.65 מודד עד כמה המודל מדרג נכון זוגות מטופלים לפי סיכון (0.5 = ניחוש, 1.0 = מושלם).

**תראי על המסך:** `cph.plot_partial_effects_on_outcome` — עקומת שרידות חזויה לגברים מול נשים.

תסבירי: המודל מעולם לא הגדיר במפורש את `h_0(t)`, אז איך אפשר לצייר עקומה בכלל? דרך **אומדן ברסלו (Breslow)** — אחרי שמצאנו את המקדמים, מעריכים את הסיכון הבסיסי המצטבר בנפרד מתוך אותו דאטה, ומשלבים.

### בדיקת הנחת היסוד של המודל

**תראי על המסך:** `cph.check_assumptions()`.

תסבירי: המודל מניח שהאפקט של כל משתנה על לוג-הסיכון נשאר **קבוע לאורך זמן**. שאריות Schoenfeld מתוקננות בודקות בדיוק את זה — לכל תצפית, כמה הערך שלה סטה ממה שהמודל ציפה לו *באותו רגע ספציפי*. אם רואים מגמה לאורך זמן בגרף הזה, זה סימן שההנחה מופרת עבור אותו משתנה, וצריך מודל מתקדם יותר (כמו מקדם משתנה בזמן, או שכבור לפי אותו משתנה).

---

## סיכום (דקה 19:00–21:00)

**תראי על המסך:** פסקת הסיכום בסוף המחברת.

לסיכום: עברתי על כל הכלים ממחברת ה-lifelines המקורית — `plot_lifetimes`, התאמת MLE עם ובלי צנזורה (עם הדגמה כמותית אמיתית של ההטיה, לא רק טענה תיאורטית), Weibull ו-Exponential, המלכודת של ECDF נאיבי, קפלן-מאייר עם רצועות גרינווד, Nelson-Aalen, מבחן log-rank, רגרסיית קוקס עם משתנים אמיתיים, ובדיקת ההנחות שלה — הכל על דאטה רפואי אמיתי במקום זבובי פירות, כולל תיקון של פרשנות שגויה שלי עצמי (רו של Weibull) בדיוק כדי להראות שבדיקה ביקורתית היא לא רק סיסמה.

זהו סוף הסרטון השלישי, וסוף העבודה על שלוש המחברות.
